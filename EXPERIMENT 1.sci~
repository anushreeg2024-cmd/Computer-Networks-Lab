clc;
clear;
scf(1);
NameOfNetwork = "Hybrid Topology (Bus+Star+ Tree)";
NumberOfNodes =25;
X = [
200 300 400 500 600 700 800 900 ... 
500 450-550-500-450-550-480 520
600 550 650 520 580 620 680 700 750
];
y = [
500 500 500 500 500 500 500 500... 
650 700 700 600 600 600 750 750 
350-300 300 250 250 250 250 200 200
];
tail = [1-2-3-4-5-6-7];
head [2 3 4 5 6 7 8];

tail = [tail 9 10 11 12 13 14 15 16]; 
head = [head 4 4 4 4 4 4 4 4];
tail = [tail 17 18 19 20 21 22 23 24 25];
head = [head 6-6 17 17 18 18 19 19 20];
TotalEdges = length (tail);
for i=1: TotalEdges
plot([x(tail (i)) x (head (i))], [y (tail (i)) y (head (i))], 'g-', 'LineWidth', 2);
end
plot (x, y, 'ro');
for i = 1: NumberOfNodes
xstring (x(i)+6, y(i)+6, string (i));
end
for i = 1: TotalEdges
xm = (x(tail (i)) + x (head (i))) / 2;
ym = (y (tail (i)) + y (head (i))) / 2; 
xstring (xm, ym, string (i));
end
degree = zeros (1, NumberOfNodes);
for i=1: TotalEdges
degree (tail(i)) =  degree (tail (i)) + 1;
degree (head (i)) = degree (head (i)) + 1;
end
disp("Node Degree (Number of edges per node): ");
for i = 1: NumberOfNodes
disp("Node" + string (i) + " has " + string (degree (i)) + "edges");
end
[maxEdges, maxNode] =max (degree);
disp("Node with maximum edges: ");
disp("Node" + string (maxNode) + " with " + string (maxEdges) + "edges"); 
disp("Total Number of Nodes: " + string (NumberOfNodes)); 
disp("Total Number of Edges: " + string (TotalEdges));
a = gca();
a.axes_visible = "on";
a.grid = [1 1];
title(NameOfNetwork, "X-Nodes", "Y-Nodes");
